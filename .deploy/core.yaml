repositories:
  core:
    kind: GitRepository
    url: https://github.com/awalford16/home
    branch: new-home-cluster

apps:
  setup:
    path: .deploy/setup
    type: GitRepository
    repo: core
  home:
    # namespace: home
    path: .deploy/home
    type: GitRepository
    repo: core
