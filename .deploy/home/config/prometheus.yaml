server:
  persistentVolume:
    subPath: data
    storageClass: nfs

  emptyDir:
    sizeLimit: 10Gi

  statefulSet:
    enabled: true

alertmanager:
  enabled: false

prometheus-pushgateway:
  enabled: false
